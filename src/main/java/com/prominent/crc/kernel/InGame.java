package com.prominent.crc.kernel;

import com.prominent.crc.procdata.SaveData;

public class InGame {//UNFINISHED
    SaveData saveData;
    public InGame (SaveData saveData){
        this.saveData = saveData;
    }
    public int runGame(){
        return 0;
    }
}
